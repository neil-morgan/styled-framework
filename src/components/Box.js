import styled from "styled-components/macro";
import {
  color,
  space,
  flexbox,
  layout,
  border,
  typography,
} from "styled-system";

const Box = styled.div`
  ${color}
  ${space}
  ${flexbox}
  ${layout}
  ${border}
  ${typography}
`;

export default Box;
